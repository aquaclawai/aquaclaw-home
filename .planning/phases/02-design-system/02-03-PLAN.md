---
phase: 02-design-system
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/app/[lang]/design-system/page.tsx
autonomous: false
requirements: [BRAN-01, BRAN-04, BRAN-06]

must_haves:
  truths:
    - "Demo page displays all color tokens as visible swatches with labels"
    - "Demo page shows Fredoka headings, Nunito body text, Geist Mono code blocks"
    - "Demo page shows card components with heavy rounding (rounded-xl+)"
    - "Demo page shows mascot with floating animation"
    - "Demo page layout is responsive: single column on mobile (375px), multi-column on desktop (1280px)"
    - "Animations play smoothly with no layout shift (transform/opacity only)"
    - "Prose section with @tailwindcss/typography renders readable MDX-style content"
  artifacts:
    - path: "src/app/[lang]/design-system/page.tsx"
      provides: "Complete design system showcase page"
      contains: "font-display"
  key_links:
    - from: "src/app/[lang]/design-system/page.tsx"
      to: "src/app/globals.css"
      via: "Tailwind utility classes consuming @theme tokens"
      pattern: "bg-primary|text-primary|font-display|animate-float|rounded-xl"
    - from: "src/app/[lang]/design-system/page.tsx"
      to: "src/components/ui/MascotImage.tsx"
      via: "import { MascotImage }"
      pattern: "MascotImage"
---

<objective>
Create a design system demo/showcase page that renders every token, font, animation, and component from the design system. This page serves as both visual verification and a living reference for all subsequent phases.

Purpose: Proves the complete design system works end-to-end. Verifies responsive layout at mobile/tablet/desktop. Serves as a reference page for Phase 3+ development.
Output: `/en/design-system` page showing color palette, typography, animations, cards, mascot, and prose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-design-system/02-CONTEXT.md
@.planning/phases/02-design-system/02-RESEARCH.md

@src/app/globals.css
@src/app/layout.tsx
@src/components/ui/MascotImage.tsx

<interfaces>
<!-- From Plan 01 globals.css: available Tailwind utility classes -->
Colors: bg-primary, bg-primary-light, bg-primary-dark, bg-secondary, bg-secondary-light, bg-secondary-dark, bg-accent, bg-accent-light, bg-accent-dark, bg-contrast, bg-contrast-light, bg-contrast-dark, bg-card, bg-muted, bg-background, text-foreground
Fonts: font-display (Fredoka), font-sans (Nunito), font-mono (Geist Mono)
Radius: rounded-sm (0.5rem), rounded-md (0.75rem), rounded-lg (1rem), rounded-xl (1.5rem), rounded-pill (9999px)
Animations: animate-bounce-in, animate-float, animate-pop
Typography: prose, prose-lg, prose-headings:font-display, prose-a:text-primary

<!-- From Plan 02: MascotImage component -->
```tsx
import { MascotImage } from '@/components/ui/MascotImage'
// <MascotImage pose="default" size={256} className="animate-float" />
// <MascotImage pose="waving" size={128} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create design system showcase page at /[lang]/design-system</name>
  <files>src/app/[lang]/design-system/page.tsx</files>
  <read_first>
    - src/app/globals.css (all available tokens to showcase)
    - src/components/ui/MascotImage.tsx (component API)
    - src/app/[lang]/layout.tsx (parent layout structure)
    - .planning/phases/02-design-system/02-CONTEXT.md (D-01 through D-16 decisions)
  </read_first>
  <action>
Create `src/app/[lang]/design-system/page.tsx` as a Server Component (no 'use client' needed unless animations require it -- CSS animations work without client JS).

The page must include ALL of the following sections:

**Section 1: Hero with Mascot**
- Large heading "AquaClaw Design System" using `font-display text-5xl font-bold text-primary`
- Subtitle using `font-sans text-lg text-foreground/80`
- MascotImage with `pose="waving"` at `size={256}` with `className="animate-float"`
- Layout: flex-col on mobile, flex-row lg on desktop (gap-8)

**Section 2: Color Palette**
- Heading "Color Palette" with `font-display text-3xl`
- Grid of color swatches: 4 columns on desktop (`grid-cols-2 md:grid-cols-4`), each swatch is a `div` with:
  - Background color class (e.g., `bg-primary`)
  - `rounded-xl` (per D-03 heavy rounding)
  - `h-24` height
  - Label below with hex value and token name
- Colors to show: primary (#FF6B35), primary-light, primary-dark, secondary (#F7C948), secondary-light, secondary-dark, accent (#FF8C94), accent-light, accent-dark, contrast (#2EC4B6), contrast-light, contrast-dark, background, card, muted

**Section 3: Typography Scale**
- Heading "Typography" with `font-display text-3xl`
- Display font demo: "Fredoka Display" in `font-display` at sizes text-xl through text-5xl
- Body font demo: paragraph in `font-sans` (Nunito) showing regular and bold weights
- Monospace demo: code block in `font-mono` with a sample code snippet
- Prose demo: a `<div className="prose prose-lg prose-headings:font-display prose-a:text-primary max-w-prose">` containing:
  - An h2, h3
  - A paragraph of lorem ipsum
  - A bulleted list
  - An inline code snippet
  - A link styled with text-primary

**Section 4: Border Radius & Cards**
- Heading "Cards & Shapes" with `font-display text-3xl`
- Grid of 3 demo cards (`grid-cols-1 md:grid-cols-3 gap-6`) each with:
  - `bg-card rounded-xl p-6 shadow-sm` (per D-03 heavy rounding)
  - Title in `font-display text-xl font-semibold`
  - Body text in `font-sans`
  - One card with a pill-shaped button: `bg-primary text-white rounded-pill px-6 py-2 font-display font-semibold`
- Show radius scale: sm, md, lg, xl, pill as small demo boxes

**Section 5: Animations**
- Heading "Animations" with `font-display text-3xl`
- Row of 3 demo boxes:
  1. Box with `animate-bounce-in` class -- label "Bounce In"
  2. Box with `animate-float` class -- label "Float" (shows gentle bobbing)
  3. Box with `animate-pop` class -- label "Pop" (shows on hover -- wrap with hover trigger if needed)
- MascotImage at each pose: default (animate-float), waving, thinking, sleeping -- in a 4-column grid on desktop, 2 on mobile

**Section 6: Responsive Layout Proof**
- Heading "Responsive" with `font-display text-3xl`
- A note: "Resize your browser to see layout changes"
- A grid that is: 1 col on mobile, 2 on md, 3 on lg
- Each cell shows its breakpoint context: "Mobile", "Tablet", "Desktop"

**Wrapper layout:**
- Full page: `min-h-screen bg-background text-foreground`
- Content wrapper: `max-w-7xl mx-auto px-4 md:px-8 lg:px-16 py-12`
- Sections separated by `space-y-16`
- Each section has its heading + content

**Import statement:**
```tsx
import { MascotImage } from '@/components/ui/MascotImage'
```

**generateMetadata:**
```tsx
export function generateMetadata() {
  return {
    title: 'Design System',
    description: 'AquaClaw.ai visual design system showcase',
  }
}
```
  </action>
  <verify>
    <automated>cd /Users/kuanliu/aquaclaw && test -f src/app/\[lang\]/design-system/page.tsx && grep -q "font-display" src/app/\[lang\]/design-system/page.tsx && grep -q "MascotImage" src/app/\[lang\]/design-system/page.tsx && grep -q "bg-primary" src/app/\[lang\]/design-system/page.tsx && grep -q "animate-float" src/app/\[lang\]/design-system/page.tsx && grep -q "prose" src/app/\[lang\]/design-system/page.tsx && grep -q "rounded-xl" src/app/\[lang\]/design-system/page.tsx && grep -q "rounded-pill" src/app/\[lang\]/design-system/page.tsx && npm run build 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/[lang]/design-system/page.tsx exists
    - Page contains color swatches using bg-primary, bg-secondary, bg-accent, bg-contrast
    - Page contains `font-display` class (Fredoka headings)
    - Page contains `font-sans` or default body text (Nunito)
    - Page contains `font-mono` class (Geist Mono code)
    - Page contains `prose prose-lg` class for typography plugin demo
    - Page contains `prose-headings:font-display` modifier
    - Page contains `rounded-xl` on card elements
    - Page contains `rounded-pill` on at least one button
    - Page contains `animate-float` on mascot element
    - Page contains `animate-bounce-in` on a demo element
    - Page imports MascotImage from '@/components/ui/MascotImage'
    - Page shows all 4 mascot poses
    - Page uses responsive grid classes (grid-cols-1 md:grid-cols-2 lg:grid-cols-3 or similar)
    - Page contains generateMetadata export
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Design system showcase page at /[lang]/design-system displays all color tokens, typography scale, animations, mascot poses, card components, and responsive grid layout. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify design system visual appearance</name>
  <files>src/app/[lang]/design-system/page.tsx</files>
  <action>Human visual verification of the complete design system showcase page. Run `npm run dev` and inspect at multiple viewport sizes.</action>
  <what-built>Complete design system showcase page showing the Bold & Playful aesthetic: warm color palette, Fredoka/Nunito fonts, pixel-art mascot with floating animation, rounded cards, prose typography, and responsive layout.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/en/design-system
    2. **Colors:** Verify warm palette -- orange primary, golden yellow secondary, coral accent, teal contrast on cream background
    3. **Typography:** Verify Fredoka renders on all headings (rounded, playful font), Nunito on body text (clean, readable), Geist Mono on code blocks
    4. **Cards:** Verify cards have visible heavy rounding (rounded-xl or larger), not sharp corners
    5. **Buttons:** Verify at least one pill-shaped button (fully rounded ends)
    6. **Mascot:** Verify pixel-art cat image renders crisp (no blurry edges), and the floating animation plays smoothly
    7. **Animations:** Verify bounce-in animation plays on page load, float animation bobs gently
    8. **Prose:** Verify the typography section has readable content with proper heading hierarchy, line-height, and max-width
    9. **Responsive:** Resize browser to 375px width -- layout should stack to single column with no horizontal overflow
    10. **Responsive:** At 1280px width -- layout should show multi-column grids
    11. **Dark mode:** If your system is in dark mode, verify colors switch to dark variants (dark background, light text)
    12. **Reduced motion:** Enable "Reduce motion" in OS accessibility settings -- animations should stop
    13. **Favicon:** Check browser tab shows pixel-art cat icon
  </how-to-verify>
  <verify>Visual inspection of design system page at /en/design-system across multiple viewports</verify>
  <done>User confirms the Bold & Playful design system renders correctly or provides specific feedback for adjustments</done>
  <resume-signal>Type "approved" if the design system looks correct, or describe specific issues (e.g., "headings still show Arial not Fredoka", "colors too muted", "mascot is blurry")</resume-signal>
</task>

</tasks>

<verification>
- Design system page renders at /en/design-system
- All 6 sections visible: Hero, Colors, Typography, Cards, Animations, Responsive
- Color swatches match hex values from globals.css tokens
- Font pairing visible: Fredoka headings, Nunito body, Geist Mono code
- Mascot renders crisp with floating animation
- Cards have heavy rounding per D-03
- Prose section uses @tailwindcss/typography
- Responsive layout adapts correctly at 375px, 768px, 1280px
- No horizontal overflow on any viewport
- prefers-reduced-motion disables animations
- Build passes
</verification>

<success_criteria>
- Visual confirmation: Bold & Playful aesthetic is achieved -- warm, inviting, playful but professional
- Responsive confirmation: No broken layouts at standard breakpoints
- Animation confirmation: Smooth, GPU-composited, transform/opacity only
- Typography confirmation: Readable prose, clear heading hierarchy, font pairing works
- All Phase 2 success criteria from ROADMAP.md verified
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system/02-03-SUMMARY.md`
</output>
