---
phase: 05-articles-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/articles/building-ai-agent-from-scratch.mdx
  - content/articles/understanding-large-language-models.mdx
  - content/articles/web-performance-tips-2026.mdx
  - content/articles/what-is-prompt-engineering.mdx
  - content/articles/how-this-site-was-built.mdx
  - dictionaries/en.json
  - src/components/articles/ArticleCard.tsx
  - src/components/articles/ArticleCardGrid.tsx
  - src/components/articles/ArticlePrevNext.tsx
autonomous: true
requirements:
  - ARTC-03

must_haves:
  truths:
    - "3-5 article MDX files exist in content/articles/ and all parse with valid Zod frontmatter"
    - "Each seed article is 500-1500 words, written in an approachable technical style"
    - "At least one seed article contains a fenced code block to exercise syntax highlighting"
    - "Dictionary has all articles UI strings needed for listing and detail pages"
    - "ArticleCard renders thumbnail (or mascot placeholder), title, date, excerpt, tag pills"
    - "ArticlePrevNext renders left/right navigation with article titles"
  artifacts:
    - path: "content/articles/building-ai-agent-from-scratch.mdx"
      provides: "Seed article with code blocks"
      contains: "title:"
    - path: "content/articles/understanding-large-language-models.mdx"
      provides: "Seed article about LLMs"
      contains: "title:"
    - path: "dictionaries/en.json"
      provides: "Articles UI strings"
      contains: "backToArticles"
    - path: "src/components/articles/ArticleCard.tsx"
      provides: "Card component for articles listing grid"
      min_lines: 25
    - path: "src/components/articles/ArticleCardGrid.tsx"
      provides: "Grid wrapper with responsive columns"
      min_lines: 8
    - path: "src/components/articles/ArticlePrevNext.tsx"
      provides: "Prev/next article navigation for detail page"
      min_lines: 20
  key_links:
    - from: "src/components/articles/ArticleCard.tsx"
      to: "src/components/ui/MascotImage.tsx"
      via: "import MascotImage for placeholder thumbnails"
      pattern: "import.*MascotImage"
    - from: "src/components/articles/ArticleCard.tsx"
      to: "lib/content/articles.ts"
      via: "uses ArticleEntry type"
      pattern: "ArticleEntry"

---

<objective>
Create seed article content (5 entries) and all reusable UI components for the articles section.

Purpose: Establish the content that pages will render and the presentational building blocks that route pages will compose. This mirrors the Phase 4 diary pattern — content + components first, then route wiring in Plan 02. Articles differ from diary by having no dayNumber, featuring code blocks with syntax highlighting, and using tag pills instead of "Day N" badges.

Output: 5 MDX article files, updated dictionary with articles UI strings, 3 article UI components (ArticleCard, ArticleCardGrid, ArticlePrevNext). PaginationBar is reused directly from diary components.
</objective>

<execution_context>
@/Users/shuai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shuai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-articles-section/05-CONTEXT.md
@.planning/phases/04-diary-section/04-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/content/articles.ts:
```typescript
export interface ArticleEntry extends ArticleFrontmatter {
  slug: string
  content: string
}
export function getArticleEntries(): ArticleEntry[]   // sorted newest-first by date
export function getArticleBySlug(slug: string): ArticleEntry | null
```

From lib/content/schemas.ts:
```typescript
export const ArticleFrontmatterSchema = CommonFrontmatterSchema  // no extension
// ArticleFrontmatter has: title, date, excerpt, thumbnail?, slug?, tags[]
// NO dayNumber field — this is the key difference from diary
```

From src/components/ui/MascotImage.tsx:
```typescript
type MascotPose = 'default' | 'waving' | 'thinking' | 'sleeping'
interface MascotImageProps {
  pose?: MascotPose
  size?: number
  className?: string
  alt?: string
}
export function MascotImage({ pose, size, className, alt }: MascotImageProps)
```

From src/components/diary/PaginationBar.tsx (REUSE DIRECTLY — no article-specific copy needed):
```typescript
interface PaginationBarProps {
  currentPage: number
  totalPages: number
  lang: string
  basePath: string  // pass "articles" for articles section
}
export function PaginationBar({ currentPage, totalPages, lang, basePath }: PaginationBarProps)
```

From src/components/diary/DiaryCard.tsx (REFERENCE for card pattern — do NOT import, create article-specific version):
```typescript
// Uses DiaryEntry with dayNumber for deterministic pose selection
// Articles don't have dayNumber — use slug hash or index for determinism
```

From dictionaries/en.json (existing articles keys):
```json
"articles": {
  "title": "Articles",
  "description": "Technical deep-dives and explorations"
}
```

Established design token patterns (from globals.css / diary components):
```tsx
// Card
<div className="bg-card rounded-xl shadow-sm hover:shadow-md transition-shadow overflow-hidden">...</div>

// Tag pill
<span className="bg-muted text-foreground/60 text-xs px-2 py-0.5 rounded-pill">{tag}</span>

// Title
<p className="font-display text-lg font-semibold text-foreground">Title</p>

// Subtitle/excerpt
<p className="font-sans text-sm text-foreground/70">Excerpt text</p>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed article content and dictionary keys</name>
  <files>
    content/articles/building-ai-agent-from-scratch.mdx,
    content/articles/understanding-large-language-models.mdx,
    content/articles/web-performance-tips-2026.mdx,
    content/articles/what-is-prompt-engineering.mdx,
    content/articles/how-this-site-was-built.mdx,
    dictionaries/en.json
  </files>
  <action>
**Seed articles (ARTC-03, D-10, D-11, D-12):**

Create 5 MDX files in `content/articles/`. Each article:
- Frontmatter fields: `title`, `date` (sequential days starting 2026-03-25), `excerpt` (1-2 sentences), `tags` (array of strings). No `thumbnail` field (mascot placeholder per D-03). No `dayNumber` — articles use CommonFrontmatterSchema.
- 500-1500 words, approachable technical style (D-12). Not cat persona — use a friendly, knowledgeable voice suitable for the general public. Think "explainer article" not "diary entry."
- Standard markdown + code blocks (D-11). NO embedded React components.
- At least 2 articles MUST contain fenced code blocks (with language tags like ```typescript, ```python, ```bash) to exercise rehype-pretty-code syntax highlighting (D-09).

**Article topics and slugs:**

1. `building-ai-agent-from-scratch.mdx` — "Building an AI Agent from Scratch" — Explains what autonomous AI agents are, basic architecture (perception-planning-action loop), includes a Python/pseudocode snippet showing a simple agent loop. Tags: [ai, agents, tutorial]. ~800 words.

2. `understanding-large-language-models.mdx` — "Understanding Large Language Models" — Plain-language explainer of how LLMs work (tokens, attention, training). No code blocks needed — conceptual article. Tags: [ai, llm, explainer]. ~700 words.

3. `web-performance-tips-2026.mdx` — "Web Performance Tips for 2026" — Practical tips for fast websites: image optimization, code splitting, edge rendering. Includes TypeScript/Next.js code snippets. Tags: [web, performance, nextjs]. ~900 words.

4. `what-is-prompt-engineering.mdx` — "What Is Prompt Engineering?" — Beginner-friendly guide to prompt engineering with example prompts in code blocks. Tags: [ai, prompts, tutorial]. ~600 words.

5. `how-this-site-was-built.mdx` — "How This Site Was Built" — Behind-the-scenes look at AquaClaw.ai's tech stack (Next.js 16, Tailwind v4, MDX, i18n). Includes config/code snippets. Tags: [web, nextjs, behind-the-scenes]. ~1000 words.

**Dictionary keys (mirror diary pattern):**

Read `dictionaries/en.json`, then update the `articles` key (preserve existing `title` and `description`):
```json
"articles": {
  "title": "Articles",
  "description": "Technical deep-dives and explorations",
  "backToArticles": "Back to Articles",
  "page": "Page",
  "of": "of",
  "prev": "Older Article",
  "next": "Newer Article",
  "readArticle": "Read article",
  "noArticles": "No articles yet. Check back soon!"
}
```
  </action>
  <verify>
    <automated>cd /Users/shuai/workspace/citronetic/aquaclaw-home && npx vitest run --reporter=verbose src/__tests__/content.test.ts</automated>
  </verify>
  <done>5 article MDX files exist in content/articles/ with valid frontmatter (content.test.ts getArticleEntries now returns 5 entries instead of empty array). en.json has all articles UI strings. At least 2 articles contain fenced code blocks. Each article is 500-1500 words in approachable technical style.</done>
</task>

<task type="auto">
  <name>Task 2: Article UI components</name>
  <files>
    src/components/articles/ArticleCard.tsx,
    src/components/articles/ArticleCardGrid.tsx,
    src/components/articles/ArticlePrevNext.tsx
  </files>
  <action>
Create `src/components/articles/` directory with 3 server components. NO `'use client'` directives — all hover/transition effects via CSS utilities (same pattern as diary components).

**ArticleCard.tsx (D-01, D-02, D-03):**
- Props: `entry: ArticleEntry`, `lang: string`
- Import `ArticleEntry` type from `../../../lib/content/articles` (relative path — lib/ is at project root, not inside src/).
- Import `MascotImage` from `@/components/ui/MascotImage`.
- Renders a `<Link>` wrapping the entire card to `/${lang}/articles/${entry.slug}`.
- Thumbnail area at top: If `entry.thumbnail` exists, render `<Image>`. If not (D-03), render `<MascotImage>` with deterministic pose. Since articles have no `dayNumber`, use a simple hash of the slug for determinism: `const hash = entry.slug.split('').reduce((acc, c) => acc + c.charCodeAt(0), 0)`. Pose: `MASCOT_POSES[hash % MASCOT_POSES.length]`. Background: `THUMBNAIL_BG_CLASSES[hash % THUMBNAIL_BG_CLASSES.length]`. Same color palette as diary: `['bg-primary/20', 'bg-secondary/30', 'bg-accent/20', 'bg-contrast/20']`.
- Below thumbnail: Tag pills row (same style as diary: bg-muted text-foreground/60 text-xs px-2 py-0.5 rounded-pill). NO "Day N" badge — articles don't have dayNumber (D-02).
- Title in font-display text-lg font-semibold text-foreground
- Date formatted: `new Date(entry.date).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })`
- Excerpt in font-sans text-sm text-foreground/70, line-clamp-3
- Card styling: bg-card rounded-xl shadow-sm hover:shadow-md transition-shadow overflow-hidden (same as DiaryCard)
- Card should be wrapped with `group` class on the Link for group hover effects.

**ArticleCardGrid.tsx (D-01):**
- Props: `children: ReactNode`
- Identical to DiaryCardGrid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
- Note: We create an article-specific version rather than importing DiaryCardGrid to keep sections decoupled. The implementation is trivial (one div).

**ArticlePrevNext.tsx (D-08):**
- Props: `prevEntry: { slug: string; title: string } | null`, `nextEntry: { slug: string; title: string } | null`, `lang: string`, `dict: { prev: string; next: string }`
- Note the difference from DiaryPrevNext: NO `dayNumber` in EntryRef, NO `day` in dict — articles don't show "Day N" prefix.
- Full-width bar with prev on left, next on right. Same layout pattern as DiaryPrevNext.
- Left: if prevEntry, `<Link>` to `/${lang}/articles/${prevEntry.slug}` showing label "Older Article" (dict.prev) and title. Right: if nextEntry, `<Link>` showing "Newer Article" (dict.next) and title.
- Styling: same as DiaryPrevNext — bg-card rounded-xl px-4 py-3 hover:bg-muted transition-colors shadow-sm, flex justify-between gap-4, group hover for text-primary transition.
  </action>
  <verify>
    <automated>cd /Users/shuai/workspace/citronetic/aquaclaw-home && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>All 3 components compile without TypeScript errors. ArticleCard uses MascotImage for placeholder thumbnails with deterministic pose/color via slug hash. All components are server components (no 'use client'). Components use established design tokens. PaginationBar from diary is NOT duplicated — will be imported directly in Plan 02 route pages.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose src/__tests__/content.test.ts` — content tests pass (getArticleEntries now returns 5 entries)
- `npx tsc --noEmit` — no TypeScript errors across entire project
- All 5 seed articles are 500-1500 words in approachable technical style
- At least 2 articles contain fenced code blocks with language tags
- en.json has complete articles UI strings
</verification>

<success_criteria>
- 5 MDX article files exist with valid frontmatter, approachable technical voice, standard markdown
- At least 2 articles contain code blocks for syntax highlighting verification
- en.json articles key has all UI strings (backToArticles, page, of, prev, next, readArticle, noArticles)
- ArticleCard renders thumbnail/mascot-placeholder, tag pills, title, date, excerpt
- ArticleCardGrid provides responsive 1/2/3 column grid
- ArticlePrevNext renders prev/next article links (no dayNumber)
- PaginationBar from diary/ is reusable without modification (basePath="articles")
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-articles-section/05-01-SUMMARY.md`
</output>
