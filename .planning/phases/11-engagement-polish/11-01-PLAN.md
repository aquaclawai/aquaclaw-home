---
phase: 11-engagement-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/engagement/GiscusComments.tsx
  - src/app/[lang]/diary/[slug]/page.tsx
  - src/app/[lang]/articles/[slug]/page.tsx
  - .env.example
  - src/__tests__/engagement.test.ts
autonomous: false
requirements: [ENGG-01]
user_setup:
  - service: giscus
    why: "GitHub Discussions-backed comment system for diary and article pages"
    env_vars:
      - name: NEXT_PUBLIC_GISCUS_REPO_ID
        source: "https://giscus.app — enter repo 'aquaclawai/aquaclaw-home', copy repoId from generated config"
      - name: NEXT_PUBLIC_GISCUS_CATEGORY_ID
        source: "https://giscus.app — select 'Comments' category (or create one), copy categoryId from generated config"
    dashboard_config:
      - task: "Enable GitHub Discussions on aquaclawai/aquaclaw-home"
        location: "GitHub repo Settings -> Features -> check 'Discussions'"
      - task: "Create a 'Comments' category in Discussions"
        location: "GitHub repo Discussions tab -> Categories -> New category"

must_haves:
  truths:
    - "A Giscus comment widget loads at the bottom of diary detail pages"
    - "A Giscus comment widget loads at the bottom of article detail pages"
    - "The comment widget uses lazy loading to avoid impacting page performance"
    - "The widget renders correctly when NEXT_PUBLIC_GISCUS_REPO_ID and NEXT_PUBLIC_GISCUS_CATEGORY_ID are set"
  artifacts:
    - path: "src/components/engagement/GiscusComments.tsx"
      provides: "'use client' Giscus wrapper component"
      contains: "'use client'"
    - path: "src/app/[lang]/diary/[slug]/page.tsx"
      provides: "Diary detail page with Giscus embedded"
      contains: "GiscusComments"
    - path: "src/app/[lang]/articles/[slug]/page.tsx"
      provides: "Article detail page with Giscus embedded"
      contains: "GiscusComments"
    - path: ".env.example"
      provides: "Placeholder env vars for Giscus config"
      contains: "NEXT_PUBLIC_GISCUS_REPO_ID"
  key_links:
    - from: "src/components/engagement/GiscusComments.tsx"
      to: "@giscus/react"
      via: "import Giscus from '@giscus/react'"
      pattern: "import.*@giscus/react"
    - from: "src/app/[lang]/diary/[slug]/page.tsx"
      to: "src/components/engagement/GiscusComments.tsx"
      via: "import and render after DiaryPrevNext"
      pattern: "GiscusComments"
    - from: "src/app/[lang]/articles/[slug]/page.tsx"
      to: "src/components/engagement/GiscusComments.tsx"
      via: "import and render after ArticlePrevNext"
      pattern: "GiscusComments"
---

<objective>
Install `@giscus/react` and embed a GitHub Discussions-backed comment widget on diary and article detail pages.

Purpose: Enable reader engagement on content pages -- the last interactive feature before v1 ships. Giscus provides zero-backend comments stored as GitHub Discussions on `aquaclawai/aquaclaw-home`.

Output: `GiscusComments` client component, updated diary and article detail pages, env var placeholders, engagement tests.
</objective>

<execution_context>
@/Users/shuai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shuai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-engagement-polish/11-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/app/[lang]/diary/[slug]/page.tsx (insertion point -- GiscusComments goes after DiaryPrevNext, before closing </div>):
```tsx
      <DiaryPrevNext
        prevEntry={prevEntry ? { slug: prevEntry.slug, title: prevEntry.title, dayNumber: prevEntry.dayNumber } : null}
        nextEntry={nextEntry ? { slug: nextEntry.slug, title: nextEntry.title, dayNumber: nextEntry.dayNumber } : null}
        lang={lang}
        dict={{ prev: dict.diary.prev, next: dict.diary.next, day: dict.diary.day }}
      />
      {/* INSERT GiscusComments HERE */}
    </div>
  )
```

From src/app/[lang]/articles/[slug]/page.tsx (insertion point -- GiscusComments goes after ArticlePrevNext, before closing </div>):
```tsx
      <ArticlePrevNext
        prevEntry={prevEntry ? { slug: prevEntry.slug, title: prevEntry.title } : null}
        nextEntry={nextEntry ? { slug: nextEntry.slug, title: nextEntry.title } : null}
        lang={lang}
        dict={{ prev: dict.articles.prev, next: dict.articles.next }}
      />
      {/* INSERT GiscusComments HERE */}
    </div>
  )
```

From src/app/layout.tsx (root metadata object):
```typescript
export const metadata: Metadata = {
  metadataBase: new URL(
    process.env.VERCEL_URL ? `https://${process.env.VERCEL_URL}` : 'http://localhost:3000'
  ),
  title: { template: '%s | AquaClaw.ai', default: 'AquaClaw.ai — An AI-Operated Website' },
  openGraph: { siteName: 'AquaClaw.ai', type: 'website' },
}
```

From .env.example (existing content to preserve):
```
# Secret token for ISR revalidation webhook (used by OpenClaw)
REVALIDATION_SECRET=change-me-to-a-secure-random-string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @giscus/react, create GiscusComments component, embed in detail pages, add tests</name>
  <files>
    package.json
    src/components/engagement/GiscusComments.tsx
    src/app/[lang]/diary/[slug]/page.tsx
    src/app/[lang]/articles/[slug]/page.tsx
    .env.example
    src/__tests__/engagement.test.ts
  </files>
  <action>
1. Install `@giscus/react`:
   ```bash
   npm install @giscus/react
   ```

2. Create `src/components/engagement/GiscusComments.tsx` as a `'use client'` component:
   - Import `Giscus` from `@giscus/react`
   - Accept props: `{ lang: string }`
   - Read `process.env.NEXT_PUBLIC_GISCUS_REPO_ID` and `process.env.NEXT_PUBLIC_GISCUS_CATEGORY_ID`
   - If either env var is missing/empty, render nothing (return `null`) -- this prevents broken iframes during development
   - Otherwise render `<Giscus>` with these props:
     - `repo="aquaclawai/aquaclaw-home"` (per user decision D-02)
     - `repoId={process.env.NEXT_PUBLIC_GISCUS_REPO_ID}`
     - `category="Comments"`
     - `categoryId={process.env.NEXT_PUBLIC_GISCUS_CATEGORY_ID}`
     - `mapping="pathname"` (maps each page URL to a unique Discussion)
     - `strict="0"`
     - `reactionsEnabled="1"`
     - `emitMetadata="0"`
     - `inputPosition="top"`
     - `theme="preferred_color_scheme"` (matches site's CSS prefers-color-scheme, per research)
     - `lang={lang}`
     - `loading="lazy"` (critical for Lighthouse -- defers iframe until near-viewport)
   - Wrap in a `<section>` with `className="mt-16 pt-8 border-t border-muted"` for visual separation from PrevNext

3. Update `src/app/[lang]/diary/[slug]/page.tsx`:
   - Add import: `import { GiscusComments } from '@/components/engagement/GiscusComments'`
   - Insert `<GiscusComments lang={lang} />` after the `<DiaryPrevNext>` component and before the closing `</div>` of the page wrapper

4. Update `src/app/[lang]/articles/[slug]/page.tsx`:
   - Add import: `import { GiscusComments } from '@/components/engagement/GiscusComments'`
   - Insert `<GiscusComments lang={lang} />` after the `<ArticlePrevNext>` component and before the closing `</div>` of the page wrapper

5. Append to `.env.example` (preserve existing content):
   ```
   # Giscus comment system (get values from https://giscus.app after enabling GitHub Discussions)
   NEXT_PUBLIC_GISCUS_REPO_ID=
   NEXT_PUBLIC_GISCUS_CATEGORY_ID=
   ```

6. Create `src/__tests__/engagement.test.ts` with tests:
   - Test that `src/components/engagement/GiscusComments.tsx` exists and contains `'use client'` directive
   - Test that `src/components/engagement/GiscusComments.tsx` contains `loading="lazy"` (performance guard)
   - Test that `src/app/[lang]/diary/[slug]/page.tsx` contains `GiscusComments` import
   - Test that `src/app/[lang]/articles/[slug]/page.tsx` contains `GiscusComments` import
   - Use `existsSync` and `readFileSync` pattern from existing `content.test.ts`

Note: Do NOT embed Giscus on science detail pages -- locked decision D-01 specifies only diary and article pages.
  </action>
  <verify>
    <automated>cd /Users/shuai/workspace/citronetic/aquaclaw-home && npx vitest run --reporter=verbose src/__tests__/engagement.test.ts && npm run build</automated>
  </verify>
  <done>
    - `@giscus/react` is in package.json dependencies
    - `GiscusComments.tsx` exists as a `'use client'` component with lazy loading
    - Diary detail page imports and renders GiscusComments after DiaryPrevNext
    - Article detail page imports and renders GiscusComments after ArticlePrevNext
    - `.env.example` has NEXT_PUBLIC_GISCUS_REPO_ID and NEXT_PUBLIC_GISCUS_CATEGORY_ID placeholders
    - All engagement tests pass
    - Project builds without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Giscus comment widget on diary and article pages</name>
  <action>
Human verifies Giscus comment widget renders correctly on diary and article detail pages after configuring GitHub Discussions.

**What was built:** Giscus comment widget embedded on diary and article detail pages. The widget renders conditionally -- it only appears when NEXT_PUBLIC_GISCUS_REPO_ID and NEXT_PUBLIC_GISCUS_CATEGORY_ID environment variables are set.

**How to verify:**
1. Enable GitHub Discussions on `aquaclawai/aquaclaw-home`: Go to repo Settings -> Features -> check "Discussions". In the Discussions tab, create a "Comments" category (Announcement type).
2. Get Giscus config values: Visit https://giscus.app, enter repo `aquaclawai/aquaclaw-home`, select category "Comments", copy the generated `data-repo-id` and `data-category-id` values.
3. Set env vars in `.env.local`: `NEXT_PUBLIC_GISCUS_REPO_ID=<paste repo-id>` and `NEXT_PUBLIC_GISCUS_CATEGORY_ID=<paste category-id>`.
4. Run dev server: `npm run dev`.
5. Verify diary: Navigate to http://localhost:3000/en/diary/day-001 -- scroll down past prev/next links and confirm the Giscus comment widget loads.
6. Verify articles: Navigate to http://localhost:3000/en/articles/getting-started-with-aquaclaw -- scroll down and confirm the Giscus comment widget loads.
7. Verify science excluded: Navigate to http://localhost:3000/en/science/what-is-an-ai-agent -- confirm there is NO comment widget.
  </action>
  <verify>Human confirms Giscus renders on diary and article pages, and is absent from science pages</verify>
  <done>User types "approved" or describes issues to fix</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose src/__tests__/engagement.test.ts` -- all engagement tests pass
- `npm run build` -- project builds without errors
- `@giscus/react` appears in package.json dependencies
- GiscusComments.tsx uses `'use client'` directive and `loading="lazy"`
- Diary and article detail pages import and render GiscusComments
- Science detail page does NOT import GiscusComments
</verification>

<success_criteria>
- Giscus comment widget loads at the bottom of diary and article detail pages when env vars are configured
- Widget uses lazy loading (`loading="lazy"`) for Lighthouse performance
- Widget gracefully hides when env vars are not set (no broken iframes)
- No changes to science detail pages (per D-01)
</success_criteria>

<output>
After completion, create `.planning/phases/11-engagement-polish/11-01-SUMMARY.md`
</output>
