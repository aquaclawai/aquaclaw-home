---
phase: 08-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dictionaries/en.json
  - src/__tests__/homepage.test.ts
  - src/components/home/HeroSection.tsx
  - src/components/home/ValuePropGrid.tsx
  - src/components/home/FeaturedContent.tsx
autonomous: true
requirements: [HOME-01, HOME-04, HOME-05]

must_haves:
  truths:
    - "Hero section renders tagline, sub-copy, mascot, and CTA button"
    - "Value proposition grid renders 4 distinct capability sections"
    - "Featured content section renders latest articles, science, and skill packs using existing card components"
  artifacts:
    - path: "dictionaries/en.json"
      provides: "home dictionary keys for all homepage text"
      contains: "home"
    - path: "src/__tests__/homepage.test.ts"
      provides: "Homepage test scaffold"
      min_lines: 10
    - path: "src/components/home/HeroSection.tsx"
      provides: "Hero section with tagline, sub-copy, mascot, CTA"
      exports: ["HeroSection"]
    - path: "src/components/home/ValuePropGrid.tsx"
      provides: "4-section value proposition grid"
      exports: ["ValuePropGrid"]
    - path: "src/components/home/FeaturedContent.tsx"
      provides: "Featured articles, science, skill packs with existing card components"
      exports: ["FeaturedContent"]
  key_links:
    - from: "src/components/home/HeroSection.tsx"
      to: "src/components/ui/MascotImage.tsx"
      via: "import MascotImage"
      pattern: "MascotImage.*pose.*waving"
    - from: "src/components/home/FeaturedContent.tsx"
      to: "src/components/articles/ArticleCard.tsx"
      via: "import ArticleCard"
      pattern: "ArticleCard.*entry.*lang"
    - from: "src/components/home/FeaturedContent.tsx"
      to: "src/components/science/ScienceCard.tsx"
      via: "import ScienceCard with dict prop"
      pattern: "ScienceCard.*dict.*difficulty"
---

<objective>
Create the homepage dictionary keys, test scaffold, and three server-rendered section components (Hero, Value Proposition Grid, Featured Content).

Purpose: Establish the foundation that Plan 02's client islands and page orchestrator will wire together. Dictionary keys must exist before any component references `dict.home.*`. Server components are the simplest sections and can be built without complex interactivity.

Output: `dictionaries/en.json` with `home` key, homepage test file, and 3 exported server components ready for composition in `page.tsx`.
</objective>

<execution_context>
@/Users/shuai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shuai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-homepage/08-RESEARCH.md

@dictionaries/en.json
@src/app/[lang]/page.tsx
@src/components/ui/MascotImage.tsx
@src/components/diary/DiaryCard.tsx
@src/components/articles/ArticleCard.tsx
@src/components/science/ScienceCard.tsx
@src/components/skills/SkillCard.tsx
@src/app/globals.css

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/ui/MascotImage.tsx:
```typescript
type MascotPose = 'default' | 'waving' | 'thinking' | 'sleeping'
interface MascotImageProps {
  pose?: MascotPose
  size?: number
  className?: string
  alt?: string
}
export function MascotImage({ pose, size, className, alt }: MascotImageProps)
```

From src/components/articles/ArticleCard.tsx:
```typescript
interface ArticleCardProps { entry: ArticleEntry; lang: string }
export function ArticleCard({ entry, lang }: ArticleCardProps)
```

From src/components/science/ScienceCard.tsx:
```typescript
interface ScienceCardProps {
  entry: ScienceEntry
  lang: string
  dict: { difficulty: { beginner: string; intermediate: string; advanced: string } }
}
export function ScienceCard({ entry, lang, dict }: ScienceCardProps)
```

From src/components/skills/SkillCard.tsx:
```typescript
interface SkillCardProps { entry: SkillEntry; lang: string }
export function SkillCard({ entry, lang }: SkillCardProps)
```

From lib/content/ (all use `import 'server-only'`):
```typescript
// lib/content/diary.ts
export function getDiaryEntries(): DiaryEntry[]  // newest-first
// lib/content/articles.ts
export function getArticleEntries(): ArticleEntry[]
// lib/content/science.ts
export function getScienceEntries(): ScienceEntry[]
// lib/content/skills.ts
export function getSkillEntries(): SkillEntry[]
```

CSS animation tokens available in globals.css:
- `animate-bounce-in` — entrance (opacity 0->1, scale 0.8->1, 0.5s)
- `animate-float` — mascot hover float (translateY oscillation, 3s infinite)
- `animate-pop` — button press feedback (scale pulse, 0.2s)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add homepage dictionary keys and test scaffold</name>
  <files>dictionaries/en.json, src/__tests__/homepage.test.ts</files>
  <action>
1. Add a `home` key to `dictionaries/en.json` with this structure (add AFTER the `openclaw` key, before the closing `}`):

```json
"home": {
  "title": "AquaClaw.ai — An AI Agent Runs This Website",
  "description": "Watch an autonomous AI agent build, write, and operate this entire website in real time.",
  "hero": {
    "tagline": "An AI Agent Runs This Website",
    "subCopy": "Every diary entry written, every article published, every skill pack released — all by an autonomous AI. Watch it happen in real time.",
    "ctaLabel": "Explore the Diary",
    "mascotAlt": "AquaClaw mascot waving hello"
  },
  "stats": {
    "heading": "What the Agent Has Built",
    "diaryLabel": "Diary Entries",
    "articlesLabel": "Articles Published",
    "skillsLabel": "Skill Packs Released"
  },
  "carousel": {
    "heading": "Latest from the Diary",
    "prev": "Previous",
    "next": "Next",
    "viewAll": "View All Entries"
  },
  "valueProp": {
    "heading": "What Can an AI Agent Do?",
    "items": [
      { "title": "Content Creation", "body": "Writes daily diary logs, articles, and science explainers from scratch." },
      { "title": "Task Automation", "body": "Runs scheduled jobs, updates files, and manages site tasks without human intervention." },
      { "title": "Skill Building", "body": "Packages AI capabilities into reusable skill packs anyone can download." },
      { "title": "Learning in Public", "body": "Documents every action so you can see exactly how an AI thinks and works." }
    ]
  },
  "featured": {
    "articlesHeading": "Latest Articles",
    "scienceHeading": "Latest Science",
    "skillsHeading": "Latest Skill Packs",
    "viewAllArticles": "View All Articles",
    "viewAllScience": "View All Science",
    "viewAllSkills": "View All Skill Packs"
  }
}
```

2. Create `src/__tests__/homepage.test.ts` with tests:
- Test that `dictionaries/en.json` has a `home` key with nested `hero`, `stats`, `carousel`, `valueProp`, `featured` keys
- Test that `valueProp.items` has exactly 4 items, each with `title` and `body`
- Test that `getDiaryEntries()` returns a non-empty array (used for carousel and stats)
- Test that `getArticleEntries()`, `getScienceEntries()`, `getSkillEntries()` each return non-empty arrays (used for featured content and stats)

Use the same test pattern as `src/__tests__/content.test.ts`: import `{ describe, it, expect }` from `vitest`, use `readFileSync` for JSON validation, use dynamic `import()` for content accessors.
  </action>
  <verify>
    <automated>cd /Users/shuai/workspace/citronetic/aquaclaw-home && npx vitest run src/__tests__/homepage.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>en.json contains home key with all sub-keys; homepage.test.ts passes with all assertions green</done>
</task>

<task type="auto">
  <name>Task 2: Create HeroSection, ValuePropGrid, and FeaturedContent server components</name>
  <files>src/components/home/HeroSection.tsx, src/components/home/ValuePropGrid.tsx, src/components/home/FeaturedContent.tsx</files>
  <action>
Create `src/components/home/` directory with three server components. None of these use `'use client'`.

**HeroSection.tsx** (HOME-01):
- Props: `{ lang: string; dict: typeof import('dictionaries/en.json')['home'] }` — receives the full `dict.home` object.
- Layout: centered content with mascot above text. Use `animate-bounce-in` wrapper for entrance animation.
- Mascot: `<MascotImage pose="waving" size={192} className="animate-float" alt={dict.hero.mascotAlt} />` inside a `<div className="animate-bounce-in">` wrapper.
- Tagline: `dict.hero.tagline` in `font-display text-4xl md:text-5xl font-bold text-foreground` heading.
- Sub-copy: `dict.hero.subCopy` in `font-sans text-lg text-foreground/70 max-w-2xl mx-auto` paragraph.
- CTA: Link to `/${lang}/diary` using `dict.hero.ctaLabel`. Style as primary button pill: `bg-primary text-white rounded-pill px-8 py-3 font-display font-semibold text-lg hover:bg-primary-dark transition-colors inline-block`.
- Section wrapper: `<section className="text-center py-16 md:py-24 px-4">` with flex-col items-center gap-6.
- Import MascotImage from `@/components/ui/MascotImage`.

**ValuePropGrid.tsx** (HOME-04):
- Props: `{ dict: { heading: string; items: Array<{ title: string; body: string }> } }` — receives `dict.home.valueProp`.
- Layout: section heading + 2x2 grid on desktop, 1-column on mobile.
- Section heading: `dict.heading` in `font-display text-3xl font-bold text-center`.
- Grid: `grid grid-cols-1 md:grid-cols-2 gap-6 max-w-4xl mx-auto`.
- Each item is a card: `bg-card rounded-xl p-6 shadow-sm` with emoji icon (use these 4 emojis for the 4 items: pencil for Content Creation, gear for Task Automation, package for Skill Building, book for Learning in Public — rendered as large text `text-4xl mb-3`), `font-display text-xl font-semibold` title, `font-sans text-foreground/70` body.
- Section wrapper: `<section className="py-16 px-4">`.

**FeaturedContent.tsx** (HOME-05):
- Props: `{ articles: ArticleEntry[]; science: ScienceEntry[]; skills: SkillEntry[]; lang: string; dict: typeof import('dictionaries/en.json') }` — receives full dict for threading `dict.science.difficulty` to ScienceCard.
- Import existing card components: `ArticleCard` from `@/components/articles/ArticleCard`, `ScienceCard` from `@/components/science/ScienceCard`, `SkillCard` from `@/components/skills/SkillCard`.
- Import types with `import type`: `ArticleEntry` from `../../../lib/content/articles`, `ScienceEntry` from `../../../lib/content/science`, `SkillEntry` from `../../../lib/content/skills`.
- Layout: 3 sub-sections, each with a heading + 3-card grid + "View All" link.
- Each sub-section pattern:
  ```
  <div>
    <div className="flex items-center justify-between mb-6">
      <h3 className="font-display text-2xl font-bold">{heading}</h3>
      <Link href={`/${lang}/${section}`} className="text-primary font-semibold hover:underline">{viewAllLabel}</Link>
    </div>
    <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
      {entries.map(e => <Card key={e.slug} ... />)}
    </div>
  </div>
  ```
- CRITICAL: ScienceCard requires `dict={{ difficulty: dict.science.difficulty }}` prop. ArticleCard and SkillCard do NOT take a dict prop.
- Section wrapper: `<section className="py-16 px-4 max-w-6xl mx-auto space-y-16">`.
- Import Link from `next/link`.
  </action>
  <verify>
    <automated>cd /Users/shuai/workspace/citronetic/aquaclaw-home && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All three components export correctly with no TypeScript errors; HeroSection uses MascotImage with waving pose and float animation; ValuePropGrid renders 4 items from dict; FeaturedContent threads dict.science.difficulty to ScienceCard</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/homepage.test.ts --reporter=verbose` — all homepage tests pass
- `npx tsc --noEmit` — no TypeScript errors across the project
- `dictionaries/en.json` is valid JSON with `home` key containing hero, stats, carousel, valueProp, featured sub-keys
- All three components in `src/components/home/` export named functions
</verification>

<success_criteria>
- en.json has complete `home` dictionary structure with all keys needed by homepage sections
- HeroSection renders mascot (waving, floating), tagline, sub-copy, and CTA button linking to diary
- ValuePropGrid renders 4 capability cards from dict data
- FeaturedContent renders ArticleCard, ScienceCard (with dict.difficulty threading), and SkillCard using existing components
- Homepage test scaffold validates dict structure and content getter availability
- `npm run build` succeeds (no TypeScript or import errors)
</success_criteria>

<output>
After completion, create `.planning/phases/08-homepage/08-01-SUMMARY.md`
</output>
