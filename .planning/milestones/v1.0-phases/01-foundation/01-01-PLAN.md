---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.mjs
  - tsconfig.json
  - src/app/layout.tsx
  - src/app/globals.css
  - mdx-components.tsx
  - vitest.config.ts
  - src/__tests__/i18n.test.ts
  - src/__tests__/content.test.ts
autonomous: true
requirements:
  - FOUN-01

must_haves:
  truths:
    - "npm run build completes with zero TypeScript errors"
    - "npm run dev starts the Turbopack dev server without errors"
    - "The vitest test scaffold exists and runs (even if all tests are stubs/skipped)"
    - "next.config.mjs has MDX configured with Turbopack-compatible string plugin format"
    - "TypeScript strict mode is enabled with noUncheckedIndexedAccess"
  artifacts:
    - path: "package.json"
      provides: "All Phase 1 dependencies installed"
      contains: "next-intl"
    - path: "next.config.mjs"
      provides: "MDX + Turbopack config"
      contains: "remarkPlugins: ['remark-gfm']"
    - path: "tsconfig.json"
      provides: "Strict TypeScript config with path aliases"
      contains: "noUncheckedIndexedAccess"
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
      contains: "vitest"
    - path: "src/__tests__/i18n.test.ts"
      provides: "i18n test scaffold for Wave 2 tasks"
    - path: "src/__tests__/content.test.ts"
      provides: "Content layer test scaffold for Wave 2 tasks"
  key_links:
    - from: "next.config.mjs"
      to: "Turbopack MDX plugins"
      via: "string plugin names (not imported functions)"
      pattern: "remarkPlugins: \\['remark-gfm'\\]"
    - from: "tsconfig.json"
      to: "strict mode"
      via: "compilerOptions"
      pattern: "\"strict\": true"
---

<objective>
Bootstrap the AquaClaw.ai Next.js 16 project with all Phase 1 dependencies, correct TypeScript strict configuration, Turbopack-compatible MDX setup, and a vitest test scaffold that Wave 2 tasks will populate.

Purpose: Every subsequent plan depends on this project existing and building cleanly. The test scaffold provides the `<automated>` verify targets for FOUN-02, FOUN-03, FOUN-04. This plan produces a buildable skeleton — no i18n routing, no content layer, no SEO pipeline yet — just a clean foundation that compiles.

Output: A working Next.js 16 App Router project at `/Users/kuanliu/aquaclaw/` with all dependencies installed, strict TypeScript, Turbopack MDX config, and vitest scaffolded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bootstrap Next.js 16 project and install all Phase 1 dependencies</name>
  <read_first>
    - /Users/kuanliu/aquaclaw/CLAUDE.md (project constraints and stack requirements)
    - /Users/kuanliu/aquaclaw/.planning/phases/01-foundation/01-RESEARCH.md (exact package versions and installation commands)
  </read_first>
  <files>
    package.json
    next.config.mjs
    tsconfig.json
    src/app/layout.tsx
    src/app/globals.css
    mdx-components.tsx
    postcss.config.mjs
  </files>
  <action>
Run create-next-app to bootstrap the project, then install all Phase 1 dependencies.

IMPORTANT: The working directory is /Users/kuanliu/aquaclaw. The project already has CLAUDE.md and .planning/ — do NOT delete these. Bootstrap must be non-destructive (create-next-app should be run in a way that adds to the existing directory).

Step 1 — Bootstrap:
```bash
cd /Users/kuanliu/aquaclaw
npx create-next-app@latest . --typescript --eslint --app --tailwind --turbopack --no-git --yes
```
The `--yes` flag accepts defaults. If it prompts for existing directory confirmation, accept. After bootstrap, verify CLAUDE.md and .planning/ still exist.

Step 2 — Install Phase 1 dependencies:
```bash
npm install next-intl
npm install gray-matter globby zod
npm install @next/mdx @mdx-js/loader @mdx-js/react @types/mdx
npm install remark-gfm rehype-pretty-code
npm install sharp
npm install -D vitest @vitejs/plugin-react @types/node
```

Step 3 — Configure next.config.mjs with MDX and Turbopack-compatible plugin strings (CRITICAL: plugins MUST be strings, not imported functions — Turbopack requirement per research pitfall 2):
```javascript
// next.config.mjs
import createMDX from '@next/mdx'

/** @type {import('next').NextConfig} */
const nextConfig = {
  pageExtensions: ['js', 'jsx', 'mdx', 'ts', 'tsx'],
  images: {
    remotePatterns: [],
  },
}

const withMDX = createMDX({
  options: {
    remarkPlugins: ['remark-gfm'],
    rehypePlugins: ['rehype-pretty-code'],
  },
})

export default withMDX(nextConfig)
```

Step 4 — Update tsconfig.json to add strict settings and path alias. Merge these into the existing create-next-app tsconfig (do NOT replace wholesale — just add/update these compilerOptions keys):
```json
{
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

Step 5 — Create mdx-components.tsx at project root (required by @next/mdx):
```typescript
// mdx-components.tsx
import type { MDXComponents } from 'mdx/types'

export function useMDXComponents(components: MDXComponents): MDXComponents {
  return {
    ...components,
  }
}
```

Step 6 — Replace the default src/app/layout.tsx with a minimal version that includes the metadataBase (FOUN-05 requirement, per pitfall 4):
```typescript
// src/app/layout.tsx
import type { Metadata } from 'next'
import './globals.css'

export const metadata: Metadata = {
  metadataBase: new URL(
    process.env.VERCEL_URL
      ? `https://${process.env.VERCEL_URL}`
      : 'http://localhost:3000'
  ),
  title: {
    template: '%s | AquaClaw.ai',
    default: 'AquaClaw.ai — An AI-Operated Website',
  },
  openGraph: {
    siteName: 'AquaClaw.ai',
    type: 'website',
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
```

Step 7 — Verify the build passes before proceeding to Task 2:
```bash
npm run build
```
If build fails, investigate and fix — do not proceed to Task 2 until `npm run build` exits 0.
  </action>
  <verify>
    <automated>cd /Users/kuanliu/aquaclaw && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `package.json` contains `"next-intl"` in dependencies
    - `package.json` contains `"gray-matter"` in dependencies
    - `package.json` contains `"zod"` in dependencies
    - `package.json` contains `"vitest"` in devDependencies
    - `next.config.mjs` contains `remarkPlugins: ['remark-gfm']` (string form, not imported function)
    - `tsconfig.json` contains `"noUncheckedIndexedAccess": true`
    - `tsconfig.json` contains `"strict": true`
    - `mdx-components.tsx` exists at project root and contains `useMDXComponents`
    - `/Users/kuanliu/aquaclaw/CLAUDE.md` still exists (not deleted by bootstrap)
    - `/Users/kuanliu/aquaclaw/.planning/` directory still exists
  </acceptance_criteria>
  <done>Project builds cleanly, all Phase 1 dependencies installed, strict TypeScript configured, MDX configured for Turbopack, mdx-components.tsx exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create vitest configuration and test scaffolds for Wave 2 plans</name>
  <read_first>
    - /Users/kuanliu/aquaclaw/.planning/phases/01-foundation/01-VALIDATION.md (exact test file names and what each must test)
    - /Users/kuanliu/aquaclaw/package.json (verify vitest is installed before writing config)
    - /Users/kuanliu/aquaclaw/tsconfig.json (check paths alias to use correct import style)
  </read_first>
  <files>
    vitest.config.ts
    src/__tests__/i18n.test.ts
    src/__tests__/content.test.ts
  </files>
  <action>
Create the vitest configuration and two test scaffold files. These stubs allow Wave 2 plans to have `<automated>` verify commands from the start (Nyquist compliance).

Step 1 — Create vitest.config.ts at project root:
```typescript
// vitest.config.ts
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'node',
    globals: true,
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
```

Step 2 — Create src/__tests__/i18n.test.ts (stub for FOUN-02 — Wave 2 plan 02 will fill in real assertions):
```typescript
// src/__tests__/i18n.test.ts
// Tests for FOUN-02: i18n routing architecture with next-intl
// These tests verify the structure and exports of the i18n layer.
// Stub: failing tests will be replaced by real assertions in plan 01-02.

import { describe, it, expect } from 'vitest'

describe('i18n architecture (FOUN-02)', () => {
  it.todo('middleware.ts exports a default function (next-intl createMiddleware)')
  it.todo('getDictionary returns English strings for locale "en"')
  it.todo('getDictionary contains site.name key')
  it.todo('dictionaries/en.json exists and is valid JSON')
  it.todo('app/[lang]/layout.tsx exists')
})
```

Step 3 — Create src/__tests__/content.test.ts (stub for FOUN-03 and FOUN-04 — Wave 2 plan 03 will fill in real assertions):
```typescript
// src/__tests__/content.test.ts
// Tests for FOUN-03: content storage structure
// Tests for FOUN-04: typed content access layer
// Stub: failing tests will be replaced by real assertions in plan 01-03.

import { describe, it, expect } from 'vitest'

describe('content storage structure (FOUN-03)', () => {
  it.todo('content/diary/ directory exists')
  it.todo('content/articles/ directory exists')
  it.todo('content/science/ directory exists')
  it.todo('content/skills/ directory exists')
  it.todo('day-001.mdx exists in content/diary/ with valid frontmatter')
})

describe('content access layer (FOUN-04)', () => {
  it.todo('getDiaryEntries returns an array')
  it.todo('getDiaryEntries returns day-001 entry with title field')
  it.todo('getDiaryEntry("day-001") returns the correct entry')
  it.todo('DiaryFrontmatterSchema rejects entry missing required dayNumber field')
  it.todo('lib/content/diary.ts imports server-only guard')
})
```

Step 4 — Add a vitest script to package.json:
```json
"scripts": {
  "test": "vitest run --reporter=verbose",
  "test:watch": "vitest --reporter=verbose"
}
```
Use the Read tool to read the current package.json scripts section, then use the Edit tool to add the test scripts without removing existing scripts.

Step 5 — Verify vitest runs without crashing (all tests are `.todo` stubs so no failures expected):
```bash
cd /Users/kuanliu/aquaclaw && npx vitest run --reporter=verbose 2>&1
```
  </action>
  <verify>
    <automated>cd /Users/kuanliu/aquaclaw && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` exists at project root and contains `defineConfig`
    - `src/__tests__/i18n.test.ts` exists and contains `describe('i18n architecture (FOUN-02)'`
    - `src/__tests__/content.test.ts` exists and contains `describe('content storage structure (FOUN-03)'`
    - `src/__tests__/content.test.ts` contains `describe('content access layer (FOUN-04)'`
    - `package.json` scripts section contains `"test": "vitest run --reporter=verbose"`
    - `npx vitest run --reporter=verbose` exits without a process crash (todo tests are not failures — exit code 0 expected)
  </acceptance_criteria>
  <done>Vitest configured, both test scaffold files created with todo stubs, test script added to package.json. Wave 2 plans can reference these files with real assertions.</done>
</task>

</tasks>

<verification>
Full verification after Wave 1 completes:
```bash
cd /Users/kuanliu/aquaclaw && npm run build && npx vitest run --reporter=verbose
```
Both commands must exit 0.

Additional checks:
- `grep -n "noUncheckedIndexedAccess" /Users/kuanliu/aquaclaw/tsconfig.json` — must print the line
- `grep -n "remark-gfm" /Users/kuanliu/aquaclaw/next.config.mjs` — must show string form, not import
- `ls /Users/kuanliu/aquaclaw/mdx-components.tsx` — must exist
- `ls /Users/kuanliu/aquaclaw/CLAUDE.md` — must still exist (bootstrap did not delete it)
</verification>

<success_criteria>
1. `npm run build` exits 0 with no TypeScript errors
2. `npx vitest run` exits 0 (todo stubs, no failures)
3. All Phase 1 npm dependencies present in package.json
4. next.config.mjs uses string MDX plugin format (Turbopack compatible)
5. tsconfig.json has `"strict": true` and `"noUncheckedIndexedAccess": true`
6. Test scaffolds exist at `src/__tests__/i18n.test.ts` and `src/__tests__/content.test.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template at `$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
