---
phase: 09-openclaw-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dictionaries/en.json
  - src/components/openclaw/DownloadSection.tsx
  - src/components/openclaw/FeatureList.tsx
  - src/components/openclaw/TutorialCards.tsx
  - src/app/[lang]/openclaw/page.tsx
autonomous: true
requirements: [OPCL-01, OPCL-02, OPCL-03]

must_haves:
  truths:
    - "Visitor sees distinct download CTAs for local and cloud OpenClaw variants at /en/openclaw"
    - "Visitor sees a quick-start guide with feature list and use-case examples"
    - "Visitor sees tutorial cards that link to existing articles in the Articles section"
    - "Page has correct SEO metadata (title, description, OG tags)"
  artifacts:
    - path: "dictionaries/en.json"
      provides: "openclaw section dictionary keys"
      contains: "openclaw.hero"
    - path: "src/components/openclaw/DownloadSection.tsx"
      provides: "Download CTA buttons for local and cloud variants"
      min_lines: 20
    - path: "src/components/openclaw/FeatureList.tsx"
      provides: "Feature list and use-case examples"
      min_lines: 20
    - path: "src/components/openclaw/TutorialCards.tsx"
      provides: "Tutorial cards linking to articles"
      min_lines: 15
    - path: "src/app/[lang]/openclaw/page.tsx"
      provides: "OpenClaw promotional page route"
      exports: ["generateMetadata", "default"]
  key_links:
    - from: "src/app/[lang]/openclaw/page.tsx"
      to: "dictionaries/en.json"
      via: "getDictionary"
      pattern: "getDictionary"
    - from: "src/components/openclaw/TutorialCards.tsx"
      to: "/[lang]/articles/[slug]"
      via: "Link href"
      pattern: "articles/"
    - from: "src/app/[lang]/openclaw/page.tsx"
      to: "src/components/openclaw/"
      via: "component imports"
      pattern: "import.*openclaw"
---

<objective>
Build the OpenClaw/EasyClaw promotional page at `/[lang]/openclaw` with download CTAs, a quick-start guide, and tutorial cards linking to existing articles.

Purpose: Give visitors a dedicated landing page for the AI agent technology behind the site, with clear download paths and learning resources.
Output: Fully functional `/en/openclaw` page with all three required sections (downloads, features/use-cases, tutorials).
</objective>

<execution_context>
@/Users/shuai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shuai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@dictionaries/en.json
@src/components/articles/ArticleCard.tsx
@src/components/ui/MascotImage.tsx
@lib/content/articles.ts
@src/lib/i18n/getDictionary.ts
@src/app/[lang]/skills/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/i18n/getDictionary.ts:
```typescript
export type Locale = keyof typeof dictionaries
export async function getDictionary(locale: Locale): Promise<typeof import('../../../dictionaries/en.json')>
```

From lib/content/articles.ts:
```typescript
export interface ArticleEntry extends ArticleFrontmatter {
  slug: string
  content: string
}
export function getArticleEntries(): ArticleEntry[]
```

From src/components/ui/MascotImage.tsx:
```typescript
type MascotPose = 'default' | 'waving' | 'thinking' | 'sleeping'
interface MascotImageProps {
  pose?: MascotPose
  size?: number
  className?: string
  alt?: string
}
export function MascotImage(props: MascotImageProps): JSX.Element
```

From src/components/articles/ArticleCard.tsx:
```typescript
interface ArticleCardProps {
  entry: ArticleEntry
  lang: string
}
export function ArticleCard({ entry, lang }: ArticleCardProps): JSX.Element
```

Existing articles available for tutorial linking (by slug):
- `building-ai-agent-from-scratch` — "Building an AI Agent from Scratch" (tags: ai, agents, tutorial)
- `how-this-site-was-built` — "How This Site Was Built" (tags: web, nextjs, behind-the-scenes)
- `what-is-prompt-engineering` — "What Is Prompt Engineering?" (tags: ai, prompts, tutorial)

Page route pattern (from skills page):
- `params: Promise<{ lang: string }>` — Next.js 16 async params
- `export const revalidate = 3600` — ISR
- `generateMetadata` + default export async function
- Relative import `'../../../../lib/content/articles'` for lib/content (outside src/)
- `@/` alias import for src/ components

Dictionary existing `openclaw` key:
```json
"openclaw": {
  "title": "OpenClaw",
  "description": "The autonomous agent powering this site"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dictionary keys and create OpenClaw page components</name>
  <files>dictionaries/en.json, src/components/openclaw/DownloadSection.tsx, src/components/openclaw/FeatureList.tsx, src/components/openclaw/TutorialCards.tsx</files>
  <action>
**1. Expand `openclaw` key in `dictionaries/en.json`** — add these nested keys under the existing `openclaw` object (keep existing `title` and `description`):

```json
"openclaw": {
  "title": "OpenClaw",
  "description": "The autonomous agent powering this site",
  "hero": {
    "heading": "Meet OpenClaw",
    "subheading": "The autonomous AI framework that builds and operates this entire website. No human intervention required.",
    "mascotAlt": "AquaClaw mascot presenting OpenClaw"
  },
  "download": {
    "heading": "Get Started",
    "localTitle": "OpenClaw Local",
    "localDescription": "Run the agent on your own machine. Full control, full privacy.",
    "localCta": "Download for Desktop",
    "cloudTitle": "EasyClaw Cloud",
    "cloudDescription": "Zero setup. Run the agent instantly in your browser.",
    "cloudCta": "Try in Browser",
    "comingSoon": "Coming soon — sign up for early access"
  },
  "features": {
    "heading": "What OpenClaw Can Do",
    "items": [
      { "title": "Autonomous Content Creation", "description": "Writes articles, diary entries, and science explainers from scratch using structured templates." },
      { "title": "Self-Managing Site Operations", "description": "Publishes content, updates metadata, and triggers builds — no human in the loop." },
      { "title": "Extensible Skill Packs", "description": "Modular capabilities that can be added, removed, or customized to fit any use case." },
      { "title": "Built-in Quality Checks", "description": "Validates content against schemas, checks formatting, and ensures SEO best practices before publishing." }
    ]
  },
  "useCases": {
    "heading": "Use Cases",
    "items": [
      { "title": "Personal Blog", "description": "Let an AI agent write and maintain your blog while you focus on ideas." },
      { "title": "Documentation Site", "description": "Auto-generate and update technical documentation from your codebase." },
      { "title": "Content Marketing", "description": "Scale content production with AI-powered drafting and scheduling." }
    ]
  },
  "tutorials": {
    "heading": "Learn More",
    "description": "Dive deeper into how autonomous AI agents work."
  }
}
```

**2. Create `src/components/openclaw/DownloadSection.tsx`** — Server component. Accepts `dict` prop typed as `typeof en.openclaw.download`. Renders two side-by-side cards (stacked on mobile):
- Left card: OpenClaw Local — icon area (use a simple terminal/desktop emoji or text icon like `>_`), title, description, pill-shaped CTA button linking to `#` (placeholder URL). Use `bg-primary text-white` for the button.
- Right card: EasyClaw Cloud — icon area (cloud icon or text), title, description, pill-shaped CTA button linking to `#` (placeholder URL). Use `bg-accent text-white` for the button.
- Below both cards: small centered text with `dict.comingSoon` in muted foreground color.
- Layout: `grid grid-cols-1 md:grid-cols-2 gap-6`. Each card: `bg-card rounded-xl p-6 shadow-sm` with hover shadow transition.
- Buttons are `<a>` tags styled as `inline-block px-6 py-3 rounded-pill font-semibold text-center transition-transform hover:scale-105`.

**3. Create `src/components/openclaw/FeatureList.tsx`** — Server component. Accepts `dict` prop with `features` and `useCases` shape from dictionary. Renders two subsections:
- **Features grid**: `dict.features.heading` as h2, then a `grid grid-cols-1 sm:grid-cols-2 gap-4` of feature cards. Each card: `bg-card rounded-xl p-5 shadow-sm`. Title in `font-display font-semibold text-foreground`, description in `text-sm text-foreground/70`.
- **Use Cases section**: `dict.useCases.heading` as h2 (with `mt-12` spacing), then a `grid grid-cols-1 md:grid-cols-3 gap-4` of use-case cards. Same card styling but slightly different accent — use `border-l-4 border-primary` on each card for visual distinction.

**4. Create `src/components/openclaw/TutorialCards.tsx`** — Server component. Accepts props: `entries: ArticleEntry[]`, `lang: string`, `dict: { heading: string; description: string }`. Renders:
- Section heading `dict.heading` as h2, `dict.description` as subtitle paragraph.
- Grid of `ArticleCard` components: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6`.
- Import `ArticleCard` from `@/components/articles/ArticleCard`.
- Import `type ArticleEntry` from `../../../../lib/content/articles` (use `import type` to avoid server-only guard in case this component is ever used client-side, matching established pattern).

All components are server components (no `'use client'` directive). Follow Bold & Playful aesthetic — use `font-display` for headings, `rounded-xl` for cards, `rounded-pill` for buttons.
  </action>
  <verify>
    <automated>cd /Users/shuai/workspace/citronetic/aquaclaw-home && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <done>Dictionary has all openclaw page keys. Three server components exist with correct prop types. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create OpenClaw page route with SEO and tutorial wiring</name>
  <files>src/app/[lang]/openclaw/page.tsx</files>
  <action>
Create `src/app/[lang]/openclaw/page.tsx` as a server component following the established page pattern (see skills page for reference).

**Page structure:**

```typescript
import { getDictionary, type Locale } from '@/lib/i18n/getDictionary'
import { getArticleEntries } from '../../../../lib/content/articles'
import { MascotImage } from '@/components/ui/MascotImage'
import { DownloadSection } from '@/components/openclaw/DownloadSection'
import { FeatureList } from '@/components/openclaw/FeatureList'
import { TutorialCards } from '@/components/openclaw/TutorialCards'

export const revalidate = 3600
```

**generateMetadata**: Return `{ title: dict.openclaw.title, description: dict.openclaw.description }` — same pattern as skills page.

**Page component** (`OpenClawPage`):
1. Await `params` to get `lang`, call `getDictionary`.
2. Call `getArticleEntries()` and filter to tutorial-relevant articles. Use slugs: `['building-ai-agent-from-scratch', 'how-this-site-was-built', 'what-is-prompt-engineering']`. Filter: `entries.filter(e => tutorialSlugs.includes(e.slug))`. This gives 3 cards.
3. Render page layout:

```
<div className="max-w-7xl mx-auto px-4 md:px-8 lg:px-16 py-12">
  {/* Hero section */}
  <header className="text-center mb-16">
    <MascotImage pose="waving" size={128} className="mx-auto mb-6" />
    <h1 className="font-display text-4xl md:text-5xl font-bold text-foreground mb-4">
      {dict.openclaw.hero.heading}
    </h1>
    <p className="font-sans text-lg text-foreground/70 max-w-2xl mx-auto">
      {dict.openclaw.hero.subheading}
    </p>
  </header>

  {/* Download CTAs — OPCL-01 */}
  <section className="mb-16">
    <DownloadSection dict={dict.openclaw.download} />
  </section>

  {/* Features + Use Cases — OPCL-02 */}
  <section className="mb-16">
    <FeatureList dict={{ features: dict.openclaw.features, useCases: dict.openclaw.useCases }} />
  </section>

  {/* Tutorial Cards — OPCL-03 */}
  <section>
    <TutorialCards entries={tutorialArticles} lang={lang} dict={dict.openclaw.tutorials} />
  </section>
</div>
```

4. Ensure the page renders without `'use client'` — this is a pure server component.
  </action>
  <verify>
    <automated>cd /Users/shuai/workspace/citronetic/aquaclaw-home && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>The `/en/openclaw` page builds successfully. It renders hero with mascot, two download CTA cards (local + cloud), feature list with 4 items, 3 use-case cards, and 3 tutorial cards linking to existing articles. Page has correct title and description metadata.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. The built page at `/en/openclaw` contains:
   - Hero section with mascot and heading
   - Two download CTA buttons (OpenClaw Local, EasyClaw Cloud)
   - Feature list with 4 capabilities
   - Use cases section with 3 examples
   - Tutorial cards linking to real article slugs
3. `npx tsc --noEmit` passes
</verification>

<success_criteria>
- `/en/openclaw` page renders with all three required sections (OPCL-01: downloads, OPCL-02: features/use-cases, OPCL-03: tutorial cards)
- Download buttons are visible and styled as pill buttons with distinct colors
- Tutorial cards link to actual `/en/articles/[slug]` routes
- Page has SEO metadata (title: "OpenClaw", description populated)
- All text is dictionary-driven (no hardcoded English strings in components)
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-openclaw-section/09-01-SUMMARY.md`
</output>
