---
phase: 10-automation-api-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/revalidate/route.ts
  - src/__tests__/revalidate.test.ts
  - CONTENT_GUIDE.md
autonomous: true
requirements: [AUTO-01, AUTO-02, AUTO-03]

must_haves:
  truths:
    - "Calling POST /api/revalidate with valid secret returns 200 and triggers ISR revalidation"
    - "Calling POST /api/revalidate with invalid or missing secret returns 401"
    - "Calling POST /api/revalidate with valid secret but invalid body returns 400 with descriptive error"
    - "Content directories exist with documented structure and example files"
    - "Malformed frontmatter causes Zod validation errors at build time"
  artifacts:
    - path: "src/app/api/revalidate/route.ts"
      provides: "ISR revalidation webhook endpoint"
      exports: ["POST"]
    - path: "src/__tests__/revalidate.test.ts"
      provides: "Tests for revalidation endpoint"
      min_lines: 30
    - path: "CONTENT_GUIDE.md"
      provides: "Content structure documentation for OpenClaw"
      min_lines: 50
  key_links:
    - from: "src/app/api/revalidate/route.ts"
      to: "next/cache"
      via: "revalidatePath import"
      pattern: "revalidatePath"
    - from: "src/app/api/revalidate/route.ts"
      to: "process.env.REVALIDATION_SECRET"
      via: "secret token comparison"
      pattern: "REVALIDATION_SECRET"
---

<objective>
Create the ISR revalidation webhook endpoint and document the content structure for OpenClaw autonomous operation.

Purpose: Close the autonomous publishing loop so OpenClaw can write MDX files to content directories and trigger ISR revalidation via a POST request, making new content appear on the live site without a full rebuild.

Output: Working `/api/revalidate` endpoint with secret token auth, tests, and a CONTENT_GUIDE.md documenting the content pipeline for OpenClaw.
</objective>

<execution_context>
@/Users/shuai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shuai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-automation-api-layer/10-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/content/schemas.ts:
```typescript
export const DiaryFrontmatterSchema = CommonFrontmatterSchema.extend({
  dayNumber: z.number().int().positive(),
})
export const ArticleFrontmatterSchema = CommonFrontmatterSchema // no extension
export const ScienceFrontmatterSchema = CommonFrontmatterSchema.extend({
  difficulty: z.enum(['beginner', 'intermediate', 'advanced']),
})
export const SkillFrontmatterSchema = CommonFrontmatterSchema.extend({
  category: z.string(),
  downloadUrl: z.string().url(),
})
```

CommonFrontmatterSchema fields (shared by all):
```typescript
z.object({
  title: z.string(),
  date: z.string(),
  excerpt: z.string(),
  thumbnail: z.string().optional(),
  slug: z.string().optional(), // falls back to filename
  tags: z.array(z.string()).default([]),
})
```

Existing ISR config (all content pages):
```typescript
export const revalidate = 3600 // every content page exports this
```

Test runner: vitest (import { describe, it, expect } from 'vitest')
Test location: src/__tests__/
Test command: npm test
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ISR revalidation webhook endpoint with tests</name>
  <files>src/app/api/revalidate/route.ts, src/__tests__/revalidate.test.ts, .env.example</files>
  <behavior>
    - POST /api/revalidate with missing Authorization header returns 401 JSON { error: "Missing or invalid secret" }
    - POST /api/revalidate with wrong Bearer token returns 401 JSON { error: "Missing or invalid secret" }
    - POST /api/revalidate with valid Bearer token and body { "paths": ["/en/diary"] } returns 200 JSON { revalidated: true, paths: ["/en/diary"] }
    - POST /api/revalidate with valid Bearer token and body { "paths": ["/en/diary", "/en/articles"] } revalidates multiple paths and returns 200
    - POST /api/revalidate with valid Bearer token but missing paths field returns 400 JSON { error: "..." }
    - POST /api/revalidate with valid Bearer token but empty paths array returns 400 JSON { error: "..." }
    - POST /api/revalidate with valid Bearer token but non-array paths returns 400 JSON { error: "..." }
    - GET /api/revalidate returns 405 Method Not Allowed (only POST is exported)
  </behavior>
  <action>
    1. Create `src/app/api/revalidate/route.ts` as a Next.js Route Handler:
       - Export only a POST function (no GET — Next.js will auto-return 405 for unsupported methods)
       - Read `Authorization: Bearer <token>` header and compare against `process.env.REVALIDATION_SECRET`
       - If missing or mismatch, return `NextResponse.json({ error: "Missing or invalid secret" }, { status: 401 })`
       - Parse request body as JSON, validate with inline Zod schema: `z.object({ paths: z.array(z.string()).min(1) })`
       - If body validation fails, return `NextResponse.json({ error: parsed.error.message }, { status: 400 })`
       - Call `revalidatePath(path)` from `next/cache` for each path in the array
       - Return `NextResponse.json({ revalidated: true, paths: validatedPaths }, { status: 200 })`
       - Wrap in try/catch: on error return `NextResponse.json({ error: "Revalidation failed" }, { status: 500 })`

    2. Create `src/__tests__/revalidate.test.ts`:
       - Test the POST handler directly by importing it from the route file
       - Mock `next/cache` revalidatePath using vi.mock
       - Set process.env.REVALIDATION_SECRET = 'test-secret-123' in beforeEach
       - Create Request objects with appropriate headers and body for each test case
       - Test all behavior cases listed above
       - Use dynamic import pattern consistent with existing tests: `const { POST } = await import(...)` if needed, or direct import since this is not a server-only module

    3. Create `.env.example` with:
       ```
       # Secret token for ISR revalidation webhook (used by OpenClaw)
       REVALIDATION_SECRET=change-me-to-a-secure-random-string
       ```
  </action>
  <verify>
    <automated>cd /Users/shuai/workspace/citronetic/aquaclaw-home && npm test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - POST /api/revalidate endpoint exists and handles auth, validation, and revalidation
    - All test cases pass covering 401, 400, 200, and edge cases
    - .env.example documents the required environment variable
  </done>
</task>

<task type="auto">
  <name>Task 2: Content structure documentation for OpenClaw</name>
  <files>CONTENT_GUIDE.md</files>
  <action>
    Create `CONTENT_GUIDE.md` at project root documenting the content pipeline for OpenClaw automated operation. This is the reference OpenClaw uses to know HOW to publish content. Include:

    1. **Overview** — Brief explanation that content is MDX files with YAML frontmatter, read at build time and cached via ISR

    2. **Content Directories** — Table showing all 4 directories with their paths and content type:
       - `content/diary/` — Daily AI agent activity logs
       - `content/articles/` — Long-form technical articles
       - `content/science/` — Science/education explainers
       - `content/skills/` — Skill pack descriptions

    3. **Frontmatter Schemas** — For each content type, document the required and optional fields with types. Reference `lib/content/schemas.ts` as the source of truth. Include:
       - Common fields: title (string, required), date (string YYYY-MM-DD, required), excerpt (string, required), thumbnail (string, optional), slug (string, optional — defaults to filename), tags (string[], defaults to [])
       - Diary-specific: dayNumber (positive integer, required)
       - Article-specific: no additional fields
       - Science-specific: difficulty (enum: beginner | intermediate | advanced, required)
       - Skill-specific: category (string, required), downloadUrl (valid URL string, required)

    4. **File Naming Convention** — Explain the slug/filename relationship: slug defaults to filename without .mdx extension. Recommend kebab-case filenames.

    5. **Example MDX File** — Show a complete diary entry example with frontmatter and body content

    6. **Validation** — Explain that Zod schemas in `lib/content/schemas.ts` validate all frontmatter at build time. Invalid frontmatter throws a descriptive error and fails the build.

    7. **Publishing Flow** — Step-by-step:
       1. Write .mdx file to appropriate content/ directory
       2. (Optional) Run `npm run build` locally to validate
       3. Call `POST /api/revalidate` with Bearer token and paths array to trigger ISR
       4. Content appears on site within 60 seconds

    8. **Revalidation API** — Document the endpoint:
       - URL: `POST /api/revalidate`
       - Auth: `Authorization: Bearer <REVALIDATION_SECRET>`
       - Body: `{ "paths": ["/en/diary", "/en/diary/new-entry-slug"] }`
       - Response: `{ "revalidated": true, "paths": [...] }`
       - Typical paths to revalidate: listing page + detail page + homepage

    Keep the tone concise and technical — this is a machine-readable reference, not marketing copy.
  </action>
  <verify>
    <automated>test -f /Users/shuai/workspace/citronetic/aquaclaw-home/CONTENT_GUIDE.md && wc -l /Users/shuai/workspace/citronetic/aquaclaw-home/CONTENT_GUIDE.md | awk '{print ($1 >= 50) ? "PASS: " $1 " lines" : "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>
    - CONTENT_GUIDE.md exists at project root with 50+ lines
    - Documents all 4 content types with their frontmatter schemas
    - Documents the revalidation API endpoint and publishing flow
    - Serves as the reference OpenClaw uses to publish content autonomously
  </done>
</task>

</tasks>

<verification>
1. All existing tests still pass: `npm test` (no regressions)
2. Revalidation endpoint tests pass with auth, validation, and happy path coverage
3. CONTENT_GUIDE.md accurately reflects the Zod schemas in lib/content/schemas.ts
4. `.env.example` exists with REVALIDATION_SECRET placeholder
5. Build succeeds: `npm run build` completes without errors
</verification>

<success_criteria>
- POST /api/revalidate returns 200 with valid auth and paths, 401 without auth, 400 with bad body
- All vitest tests pass including new revalidation endpoint tests
- CONTENT_GUIDE.md documents all 4 content types, frontmatter schemas, and the publishing flow
- The autonomous publishing loop is documented end-to-end: write MDX -> validate -> revalidate -> live
</success_criteria>

<output>
After completion, create `.planning/phases/10-automation-api-layer/10-01-SUMMARY.md`
</output>
